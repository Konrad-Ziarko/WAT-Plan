<UserControl x:Class="DayBoxAppointmentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="BorderElement" CornerRadius="5" BorderThickness="1" Margin="2,2,2,1">
        <Border.BorderBrush >
            #000000
        </Border.BorderBrush>
        <Border.Background >
            #000000
        </Border.Background>
        <TextBlock x:Name="DisplayText" Text="Sample Apt" FontSize="10" Padding="2" TextWrapping="Wrap" FontFamily="Tahoma" FontWeight="Bold"/>
    </Border>
</UserControl>
